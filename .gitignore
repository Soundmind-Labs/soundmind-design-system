# 1. 의존성 (가장 중요)
node_modules
.pnp
.pnp.js

# 2. 빌드 결과물 (Tsup이 만드는 폴더)
dist

# 3. Storybook 빌드 결과물 & 캐시
storybook-static
.cache

# 4. 테스트 커버리지
coverage

# 5. 환경 변수 (보안상 절대 올리면 안 됨)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 6. 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 7. OS 시스템 파일 (Mac/Windows)
.DS_Store
Thumbs.db

# 8. 에디터 설정 (선택 사항)
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea

# 9. TypeScript 증분 빌드 정보
*.tsbuildinfo